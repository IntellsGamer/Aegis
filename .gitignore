__pycache__/
*.py[cod]
.venv/
venv/
*.db
*.sqlite3
.env
uploads/
# Runtime model artifacts, if a deployment chooses to retain them locally.
/backend/models/
.pytest_cache/

# Windows alternate-data-stream sidecars
*Zone.Identifier

# Local frontend dependencies; package.json and pnpm-lock.yaml define reproducible installs.
node_modules/

# Local downloads from GitHub Actions release and APK artifact retrieval.
artifacts/
