# Screenshot Capture Notes

The Persian RTL dashboard was captured on 2026-08-15 from `/dashboard`. It shows the right-side navigation, summary metrics, recent assessments, risk trend, and activity chart.

The Persian RTL scanner was captured from `/scan`. It shows the deterministic evidence-fusion explanation, all six intake modes, and its SSRF safety-boundary message.

The verified threat map was captured from `/map`. It shows the real country-aggregate map, declared community-report provenance, time-range filters, and truthful empty state when no approved country-tagged reports exist.

The assessment casefile was captured from `/report/11`. It shows an explainable threat verdict, evidence-family contributions, localized observation explanations, recorded source evidence, and a containment checklist.

The live Persian scanner was opened in message mode for an end-to-end controlled-fictional authority-lure validation. The message input, local-only processing note, and submit action rendered correctly in Persian RTL. The submitted fixture received a threat verdict with a 0.1/100 trust score and 88% evidence confidence. Its live evidence included the new Persian authority credential-or-payment lure, plus urgency, verification-code request, identity request, brand-like hostname, and sensitive URL-keyword observations.

The scanner mode-selector dark-mode defect was rechecked after the visual update. The selected mode now uses a deep cyan-to-slate tonal surface, cyan edge, restrained glow, and high-contrast text; inactive modes retain a subdued surface but have clear hover and focus states. The design remained coherent after a live switch from URL to message mode.
